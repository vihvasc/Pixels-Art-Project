/* window.onload = function load() {
  const sectionOne = document.createElement('section');
  sectionOne.id = "color-palette";
  for (let i = 0; i < 4; i += 1) {
    const divColors = document.createElement('button');
    divColors.className = 'color';
  }
};
 */